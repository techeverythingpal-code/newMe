<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        if (DB::connection()->getDriverName() !== 'pgsql') {
            return;
        }

        DB::statement('SELECT setval(pg_get_serial_sequence(\'teacher_grades\', \'id\'), COALESCE((SELECT MAX(id) FROM teacher_grades), 1))');

        DB::statement('
            DO $$
            BEGIN
                IF NOT EXISTS (
                    SELECT 1 FROM pg_attrdef ad
                    JOIN pg_attribute a ON a.attnum = ad.adnum AND a.attrelid = ad.adrelid
                    WHERE ad.adrelid = \'teacher_grades\'::regclass AND a.attname = \'id\'
                ) THEN
                    EXECUTE \'ALTER TABLE teacher_grades ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY\';
                END IF;
            END $$;
        ');
    }

    public function down(): void
    {
        if (DB::connection()->getDriverName() !== 'pgsql') {
            return;
        }

        DB::statement('ALTER TABLE teacher_grades ALTER COLUMN id DROP IDENTITY IF EXISTS');
    }
};